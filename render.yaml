databases:
  - name: schedules-db
    databaseName: schedules
    user: schedules_user
    plan: free

services:
  - type: web
    name: schedules-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: schedules-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
